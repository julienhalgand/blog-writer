   {% extends 'layout.twig' %}
   {% block content %}
        <div class="row">
        <div class="small-12 columns">
            {{objects.content|raw}}
        </div>
        </div>
   {% endblock %}